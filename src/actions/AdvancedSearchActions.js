export const changeCheckboxes = (box) => ({
    type: "CHANGE_CHECKBOXES",
    box
});

export const changeFloors = (floors) => ({
    type: "CHANGE_FLOORS",
    floors
});

export const changeSize = (floors) => ({
    type: "CHANGE_SIZE",
    floors
});

export const changeSize = (freeText) => ({
    type: "CHANGE_FREE_TEXT",
    freeText
});

export const changeSize = (entryDate) => ({
    type: "CHANGE_ENTRY_DATE",
    entryDate
});

export const clear = () => ({
    type: "CLEAR"
});